# ─────────────────────────────────────────────────────────────────────────────
#  CI — nRF52 Factory Erase
#
#  Runs on every push to GitFlow branches (develop, feature/*, release/*,
#  hotfix/*) and on Pull Requests targeting main or develop.
#  Version tags (v*.*.*) are handled by release.yml.
#
#  Steps:
#    1. Compute version with GitVersion (GitFlow/v1 workflow)
#    2. Update version.properties with the computed version
#    3. Build both softdevice variants directly with PlatformIO
#    4. Upload raw firmware artifacts for inspection
# ─────────────────────────────────────────────────────────────────────────────
name: CI

on:
  push:
    branches:
      - develop
      - "feature/**"
      - "release/**"
      - "hotfix/**"
    tags-ignore: ["v*"]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    name: Build firmware
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (full history for GitVersion)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v3
        with:
          versionSpec: "6.x"

      - name: Compute version
        id: gitversion
        uses: gittools/actions/gitversion/execute@v3
        with:
          useConfigFile: true
          configFilePath: GitVersion.yml

      - name: Show computed version
        run: |
          echo "SemVer    : ${{ steps.gitversion.outputs.semVer }}"
          echo "FullSemVer: ${{ steps.gitversion.outputs.fullSemVer }}"
          echo "Branch    : ${{ steps.gitversion.outputs.branchName }}"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install PlatformIO
        run: pip install --no-cache-dir "platformio==6.1.15"

      - name: Build all environments
        env:
          APP_VERSION: ${{ steps.gitversion.outputs.fullSemVer }}
        run: bash scripts/linux/build-nrf52.sh all

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware-build-${{ github.sha }}
          path: build/
          if-no-files-found: error
